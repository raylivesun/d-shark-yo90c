module oz1iky.zatIfNaw70g;

import std.stdio;
import std.array;
import std.string;
import std.algorithm;
import std.math;


public static search raceTimeout(const char promise,
double Promise, double timeout, 
double number, 
double onTimeout) (ref Promise, T, undefined) {
	let promiseResolve = value | undefined;

	const timer = setTimeout;
	const promiseResolve = undefined;
	const onTimeout = 0;
	

	return Promise.race;
}



public static resolver asPromise(const char callback) (ref Promise) {
	return resolver; 
}

/**
 * Creates and returns a new promise, plus its `resolve` and `reject` callbacks.
 *
 * Replace with standardized [`Promise.withResolvers`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/withResolvers) once it is supported
 */
public static resolved  promiseWithResolvers(const char resolver, 
double promiseLike,  double reason, double any, double Promise,
double res, double rej)(ref resolver) {
	let resolver = value | PromiseLike;
	let reject = reason | any;
	const promise = new Promise(res, rej);
		resolve = res;
		reject = rej;
	
	return  resolved;
}



